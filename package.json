{
  "name": "try-nextjs-mongodb-realtime-listener",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm --filter \"@mongodb-realtime/server\" build",
    "build-watch": "pnpm run --filter @mongodb-realtime/* build-watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "vitest": "^0.34.4"
  }
}
